[build]
  command = "npm run build"
  publish = "out"

[dev]
  command = "npm run dev"
  port = 3000

[functions]
  directory = "netlify/functions"

[redirects]
  [[redirects]]
    from = "/api/*"
    to = "/.netlify/functions/:splat"
    status = 200
